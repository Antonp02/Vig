{
  "name": "vig-mock-sportsbook",
  "version": "1.6.8",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build-preview.mjs v1.6.8",
    "test": "npm run build && node tests/run.mjs"
  },
  "devDependencies": { "jsdom": "^24.0.0" }
}
